@model IEnumerable<WebApplication8.Models.Catalogy>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CatalogyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CatalogyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Sửa", new { id=item.CatalogyID }) |
            @Html.ActionLink("Details", "Chi Tiết", new { id=item.CatalogyID }) |
            @Html.ActionLink("Delete", "Xóa", new { id=item.CatalogyID })
        </td>
    </tr>
}

</table>
